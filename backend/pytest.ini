[pytest]
testpaths = tests
# Puts backend/ on sys.path so `from app.config import ...` resolves without an
# installed package. Run pytest from backend/, not the repo root.
pythonpath = .
addopts = -q --strict-markers
